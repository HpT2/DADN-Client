using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "HelmetConfig", menuName = "Configs/Items/Helmet")]
public class HelmetConfig : ItemConfig
{
    // Start is called before the first frame update
    public float def;
    public float expGain;
}
